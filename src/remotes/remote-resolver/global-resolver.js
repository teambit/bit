const remoteResolver = (name) => {
  function getResolvers() {
     // fetch all resolvers with globbing pattern form fs
  }

  const resolvers = getResolvers();
  

};

export default remoteResolver();

const bitCommunityResolver = {
  
};