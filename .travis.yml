language: node_js
node_js:
  - '6'
  - '7'

before_install:
  - npm install bit-bin
  - node --version


addons:
  apt:
    packages:
      - git

branches:
  only:
  - master

cache:
  directories:
  - node_modules
  - lib
  - lib-legacy

os:
  - linux
