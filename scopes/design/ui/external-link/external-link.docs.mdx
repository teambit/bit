---
labels: ['react', 'typescript', 'link', 'external']
description: 'A link component to display an external link with no text decoration'
---

import { ThemeCompositions } from '@teambit/documenter.theme.theme-compositions';
import { ExternalLink } from './external-link';

### Overview

A link component for attaching a link to text without text decoration

```js live
() => {
  return (
    <ThemeCompositions style={{ minHeight: 120}}>
        <ExternalLink href="https://bit.dev">some link text - take me to bit.dev!!</ExternalLink> 
    </ThemeCompositions>
  );
};
```
