---
description: "Adds a command-bar (to Bit's UI) that searches components and runs commands."
labels: ['commands', 'search', 'discovery']
---

Opens on CMD+K and allows searching for components by name. Typing '> ' inside the command bar displays the different commands and their shortcuts.
