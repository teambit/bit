import { Aspect } from '@teambit/harmony';

export const ReactRouterAspect = Aspect.create({
  id: 'teambit.ui/react-router',
  dependencies: [],
  defaultConfig: {},
});

export default ReactRouterAspect;
