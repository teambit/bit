// import { TSServer } from './ts-server';

describe('TSServer', () => {
  it('should create a new instance of tsserver', async () => {
    // const tsserver = await TSServer.create();
    // tsserver.getProjectInfo();
  });
});
