# TEMPORARY (this branch only): the real CircleCI config is replaced with a no-op
# while experimenting with running e2e tests on GitHub Actions
# (see .github/workflows/e2e-tests.yml). Do NOT merge this file to master.
version: 2.1

jobs:
  noop:
    docker:
      - image: cimg/base:stable
    resource_class: small
    steps:
      - run: echo "no-op - this branch runs its CI on GitHub Actions (e2e-tests.yml experiment)"

workflows:
  noop:
    jobs:
      - noop
