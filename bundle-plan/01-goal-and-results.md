# 1. Goal & result so far

[← back to bundle-plan index](../bundle-plan.md)

Ship the Bit CLI as a **single bundled JavaScript file** plus a thin ring of packages that genuinely cannot be inlined, instead of a 1.2 GB `node_modules` tree.

|                     | released bit (bvm 2.0.72) | bundled bit (this branch)                                                                                            |
| ------------------- | ------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| install size        | **1.2 GB**                | **234 MB** (59 MB bundle + 68 MB externals + ~107 MB shims, up from 159 MB on 2026-09-06 — see the breakdown below). |
| files on disk       | **141,008**               | **~5,673**                                                                                                           |
| `bit --help` (warm) | 0.662 s                   | 0.642 s (SEA: 1.324 s — §9)                                                                                          |
| `bit list` (warm)   | 0.914 s                   | **0.848 s** (SEA: 1.574 s)                                                                                           |
| single executable   | —                         | **179 MB** `bit-app` (+ the `bundle/` support dir)                                                                   |
| build time          | n/a                       | \~11 s esbuild + \~5 s codegen (+ \~40 s for the SEA variant)                                                        |

Every command on the target list — and a good deal more — runs from `/tmp/bit-bundle` against workspaces in `/tmp/bundle-tests/*`, with **zero** reads from this repo or from `~/.bvm` (§7.3).

### Size breakdown, 2026-09-07 (`ui-vendor-dll` branch, PR #10690, measured after `npm install`)

| Component                                                                                                                                                                                                                                                       | Size                                         | Since                                                                                                                                                               |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `bit.app.js` (the CLI bundle itself)                                                                                                                                                                                                                            | 59 MB                                        | unchanged                                                                                                                                                           |
| externals (`node_modules/`, npm-installed)                                                                                                                                                                                                                      | 68 MB                                        | unchanged                                                                                                                                                           |
| shims (`dist/core-aspects/node_modules/`)                                                                                                                                                                                                                       | ~107 MB, up from ~32 MB                      | see rows below                                                                                                                                                      |
| — pure shim files (`.aspect.js`/`.runtime.js`/redirects)                                                                                                                                                                                                        | ~15 MB                                       | unchanged                                                                                                                                                           |
| — pre-built UI/preview artifacts (`artifacts/ui-bundle/`, `ui-preview/`)                                                                                                                                                                                        | ~63 MB, up from 16.8 MB                      | see [18-findings-log.md](18-findings-log.md)'s 2026-09-06 entry for the +53 MB SSR-bundle regression inside this (unrelated to the vendor DLL, not yet root-caused) |
| — UI vendor DLL (`ui-vendor-dll/vendor.js`+`.css`+manifest)                                                                                                                                                                                                     | ~9.6 MB                                      | added [27-ui-vendor-dll-plan.md](27-ui-vendor-dll-plan.md) Tasks 3b/6                                                                                               |
| — **browser-safe barrels** (`<shim>/browser/`, real compiled `dist/` copied per shim so a browser-target bundler's _bare_ `@teambit/<aspect>` import gets real code instead of `require(bit.app.js)[...]`)                                                      | **~12 MB**, 1,726 files across 102/105 shims | **new 2026-09-07**, see [18-findings-log.md](18-findings-log.md) and `generate-shim-packages.ts`'s `copyBrowserDist`                                                |
| — **vendored harmony runtime deps** (`reflect-metadata`, `cleargraph`, `comment-json`, `user-home` + their own transitive deps — needed by `@teambit/harmony`'s real `browser/` copy, copied directly rather than round-tripped through a second `npm install`) | **~6.4 MB**, 12 packages                     | **new 2026-09-07**, see `generate-shim-packages.ts`'s `copyHarmonyRuntimeDeps`                                                                                      |
| **total**                                                                                                                                                                                                                                                       | **234 MB** / ~5,673 files                    | +75 MB / +2,861 files vs. 2026-09-06's 159 MB / 2,812                                                                                                               |

The two new rows (browser barrels + vendored harmony deps) are what actually closes
[known gap 1](14-known-gaps.md) for real: a third-party UI root's non-core aspects that bare-import a
covered core aspect (rather than importing its exact `.ui.runtime.js` file) now get real, compilable
code instead of `require(bit.app.js)[...]`, which cannot compile for any browser target at all.
Verified end-to-end against `community-cloud`/`bit-cloud`: `CLIENT_ONLY=true bit run community-cloud`
goes from the pre-existing 64-error baseline to **0 rspack errors**, confirmed executing for real in
a browser (React Router, Harmony's client bootstrap) — see [18-findings-log.md](18-findings-log.md)'s
2026-09-07 entry for the full verification.
