const descriptors = [
  {
    name: 'list',
    alias: 'ls',
    description: 'list components tracked in workspace',
    aspectId: 'teambit.workspace/workspace',
  },
];

export default descriptors;
