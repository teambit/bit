---
description: A toggle button input.
labels: ['react', 'typescript', 'toggle', 'input', 'button']
---

import { ToggleButton } from './toggle-button';

A toggle button component that can be used with two options or more.  
Basic example:

```js live
() => {
  const options = [
    { value: 'One', element: 'One' },
    { value: 'Two', element: 'Two' },
  ];
  return <ToggleButton options={options} />;
};
```

Multiple options example:

```js live
() => {
  const options = [
    { value: 'One', element: 'One' },
    { value: 'Two', element: 'Two' },
    { value: 'Three', element: 'Three' },
    { value: 'Four', element: 'Four' },
    { value: 'Five', element: 'Five' },
  ];
  return <ToggleButton options={options} />;
};
```
