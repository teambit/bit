---
labels: ['module', 'typescript', 'detective']
description: 'A function to detect the dependencies of an TypeScript file.'
---

API:

```ts
import detective from '@teambit/typescript.deps-detectors.detective-typescript';
function detective(code): string[];
```
