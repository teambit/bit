---
labels: ['module', 'es6', 'detective']
description: 'A function to detect the dependencies of an ES6 module.'
---

API:

```ts
import detective from '@teambit/node.deps-detectors.detective-es6';
function detective(code): string[];
```
